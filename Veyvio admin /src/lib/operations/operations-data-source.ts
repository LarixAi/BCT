/** Operations demo layer removed — all authenticated workspaces use live tenant-scoped API data. */
export function isOperationsDemoLayerActive(): boolean {
  return false
}

export const OPERATIONS_DEMO_BANNER = null
